# System packages for Streamlit Community Cloud
# These will be installed automatically

# For geospatial operations (lightweight)
libspatialindex-dev

# For image processing
libgdal-dev

# For system utilities
curl
wget